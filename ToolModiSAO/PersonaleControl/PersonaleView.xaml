<Window x:Class="ToolModiSAO.PersonaleControl.PersonaleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ToolModiSAO.PersonaleControl"
        Title="Gestione Personale" Height="450" Width="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding ListaPersonale}"
                  SelectedItem="{Binding PersonaleSelezionato}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">

            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick"
                              Command="{Binding ApriModificaCommand}" />
            </DataGrid.InputBindings>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"                Binding="{Binding Id}"               Width="60"/>
                <DataGridTextColumn Header="Grado/Qualifica"   Binding="{Binding GradoQualifica}"   Width="150"/>
                <DataGridTextColumn Header="Categoria/Profilo" Binding="{Binding CategoriaProfilo}" Width="150"/>
                <DataGridTextColumn Header="Cognome"           Binding="{Binding Cognome}"          Width="120"/>
                <DataGridTextColumn Header="Nome"              Binding="{Binding Nome}"             Width="120"/>
                <DataGridTextColumn Header="Mil/Civ"           Binding="{Binding MilCiv}"           Width="80"/>
                <DataGridTextColumn Header="Matricola"         Binding="{Binding Matricola}"        Width="120"/>
                <DataGridTextColumn Header="Reparto"           Binding="{Binding CodReparto}"       Width="100"/>
                <DataGridTextColumn Header="Sezione"           Binding="{Binding CodSezione}"       Width="100"/>
                <DataGridTextColumn Header="Nucleo"            Binding="{Binding CodNucleo}"        Width="100"/>
                <DataGridTextColumn Header="Ufficio"           Binding="{Binding CodUfficio}"       Width="100"/>
                <DataGridTextColumn Header="Incarico"          Binding="{Binding Incarico}"         Width="150"/>
                <DataGridTextColumn Header="Stato Servizio"    Binding="{Binding StatoServizio}"    Width="120"/>
                <DataGridTextColumn Header="Annotazioni"       Binding="{Binding Annotazioni}"      Width="200"/>
                <DataGridTextColumn Header="Attivo"            Binding="{Binding Attivo}"           Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>